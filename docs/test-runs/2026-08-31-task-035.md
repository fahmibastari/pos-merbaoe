# HASIL QA OTOMATIS — TASK-035

**Tanggal:** 31 Agustus 2026  
**Pelaksana:** Codex  
**Lingkungan:** working tree lokal; Supabase development untuk integration test;
production build lokal untuk browser E2E  
**Git/deploy:** tidak dilakukan

## Keputusan tooling D-10

- Unit dan integration test: `node:test` + `tsx` + Prisma.
- Browser E2E: Playwright Test 1.62.1, Chromium, satu worker.
- CI: GitHub Actions dengan PostgreSQL 16 sementara dan tanpa deployment.
- Maestro: opsional bila nanti ada aplikasi mobile/PWA yang perlu smoke lintas platform.
- WireMock: ditunda sampai ada integrasi API HTTP pihak ketiga.

## Cakupan yang ditambahkan

Tujuh regression test database khusus ditambahkan untuk I-01, I-02, I-03, I-04, I-05,
I-06, dan I-09. Skenario I-05 menggunakan dua request berbeda pada stok yang hanya cukup
untuk satu: satu transaksi berhasil, satu ditolak, dan stok akhir tetap nol.

Sebelas test browser mencakup:

1. redirect pengunjung tanpa sesi;
2. login Admin dan Kasir ke area perannya;
3. Kasir tidak dapat membuka `/admin/*`;
4. enam layar Admin utama;
5. pencarian menu, checkout QRIS, dan pembukaan struk;
6. Dashboard dan POS tanpa body overflow pada 1440, 768, dan 375 px.

Fixture E2E memakai awalan `e2e-merbaoe-` dan dibersihkan global setelah run. Pemeriksaan
pascarun menemukan user, menu, bahan, dan kategori fixture masing-masing nol; direktori
auth state `.playwright` juga sudah dihapus.

## Hasil final

| Gate | Hasil |
| :--- | :--- |
| ESLint seluruh repo | 0 error, 0 warning |
| TypeScript | lulus |
| Prisma schema | valid |
| Unit + integration | **82 lulus, 0 gagal, 0 skip**; 268,8 detik |
| Production build | lulus; Next.js 16.2.11; 23 route |
| Playwright | **11 lulus, 0 gagal**; 2,0 menit |
| Fixture E2E tersisa | 0 user, 0 menu, 0 bahan, 0 kategori |

Run Playwright awal pada development server menemukan koneksi Prisma yang terputus saat
perpindahan halaman. Mengikuti panduan Next.js, E2E dipindahkan ke production build;
run final stabil dan seluruh test lulus. Dua mismatch nama heading pada test diperbarui
ke copy UI aktual (`Menu` dan `Laporan`); tidak ada perubahan visual aplikasi.

## CI dan batas klaim

`.github/workflows/qa.yml` menjalankan migrasi/seed pada PostgreSQL sementara, lint,
TypeScript, Prisma validate, 82 test Node, build, dan Playwright pada push/pull request.
Workflow tidak mengakses Supabase development dan tidak melakukan deploy.

Run GitHub pertama pada branch `dev` berhasil membuat PostgreSQL dan menerapkan seluruh
10 migrasi, lalu gagal sebelum test karena `prisma/seed.ts` mengimpor direktori
`../src/generated/prisma` yang tidak diresolusi runner Linux. Import seed dan seluruh
jalur server/test diperjelas ke file `generated/prisma/client`, sesuai output generator
Prisma. Setelah koreksi, Prisma generate, smoke import, ESLint, TypeScript, schema
validate, **82/82** Node test, dan production build **23 route** lulus lokal. Status CI
push ulang kemudian hijau dalam 3 menit 34 detik: migration/seed, Chromium, lint,
TypeScript, Prisma validate, 82 test, build, dan 11 E2E seluruhnya lulus. Kegagalan run
pertama bukan kegagalan migration, PostgreSQL, secret, Supabase, atau logika bisnis.

UAT operasional minimal tiga hari, skor SUS, perbandingan laporan dengan catatan owner,
opname fisik, dan uji restore backup tetap belum dilakukan. Hasil TASK-035 tidak mengganti
gate manusia dan pemulihan tersebut.
